#ifndef VWS_PLATFORM_DECL
#define VWS_PLATFORM_DECL

#define VWS_RELEASE_VERSION "${RELEASE_VERSION}"
#define VWS_RELEASE_DATE    "${RELEASE_DATE}"

#define VWS_VERSION_MAJ     "${VERSION_MAJ}"
#define VWS_VERSION_MIN     "${VERSION_MIN}"
#define VWS_VERSION_CL      "${VERSION_CL}"
#define VWS_VERSION_PL      "${VERSION_PL}"
#define VWS_VERSION_BN      "${VERSION_BN}"
#define VWS_VERSION         "${RELEASE_VERSION}"

#endif /* VWS_PLATFORM_DECL */
